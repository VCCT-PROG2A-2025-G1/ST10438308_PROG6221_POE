<Window x:Class="CyberSecurityChatBotWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CyberSecurity ChatBot" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Chat Display -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="ChatHistoryTextBlock" TextWrapping="Wrap" FontSize="14" />
        </ScrollViewer>

        <!-- Input Area -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox x:Name="UserInputTextBox" Width="600" Height="30" Margin="0,0,10,0"/>
            <Button Content="Send" Width="100" Height="30" Click="SendButton_Click"/>
        </StackPanel>
    </Grid>
</Window>