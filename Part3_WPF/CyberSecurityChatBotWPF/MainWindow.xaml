<Window x:Class="CyberSecurityChatBotWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CyberBot" Height="500" Width="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Optional Top Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,5">
            <Button Content="📋 Open Task Manager" Width="150" Margin="5" Click="OpenTaskManager_Click"/>
            <Button Content="🧠 Start Quiz" Width="100" Margin="5" Click="OpenQuiz_Click"/>
        </StackPanel>

        <!-- Chat History -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10">
            <TextBlock x:Name="ChatHistoryTextBlock" TextWrapping="Wrap" />
        </ScrollViewer>

        <!-- Input Box -->
        <TextBox x:Name="UserInputTextBox" Grid.Row="2" Margin="0,10" Height="30"/>

        <!-- Send Button -->
        <Button Content="Send" Grid.Row="3" Click="SendButton_Click" Height="30" Width="100" HorizontalAlignment="Right" Margin="0,5"/>
    </Grid>
</Window>
